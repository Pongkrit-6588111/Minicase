sonar.projectKey=Pongkrit-6588111_Minicase
sonar.organization=pongkrit-6588111
sonar.host.url=https://sonarcloud.io

# Define the source code directory
sonar.sources=.

# Specify where compiled Java binaries are stored
sonar.java.binaries=target/classes  # If using Maven
# sonar.java.binaries=build/classes  # If using Gradle

# Exclude only test files, not all Java files
sonar.exclusions=**/test/**

# Coverage exclusions (adjust based on what you need)
sonar.coverage.exclusions=**/test/**, **/config/**

# Project metadata (optional but useful)
sonar.projectName=Minicase
sonar.projectVersion=1.0
sonar.sourceEncoding=UTF-8
